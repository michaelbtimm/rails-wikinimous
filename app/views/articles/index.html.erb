<% @articles.each do |article| %>
  <div class="card col-lg-6 mb-4 d-flex" style="width: 18rem;">
    <div class="card-body">
      <h2 class="card-header"><%= link_to article.title, article_path(article) %></h2>
      <p class="card-text"><%= article.content %></p>
    </div>
  </div>
<% end %>
